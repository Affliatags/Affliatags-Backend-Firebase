import { describe, it } from "node:test"

// const assert = require('assert')
// const user = require("../fixtures/user").user

describe("Tag Endpoint Suite", () => {
    it("Should generate a tag given that the user is authenticated", () => {
        // TODO:
    })

    it("Should fail to generate a tag given that the user is unauthorized", () => {
        // TODO:
    })

    it("Should verify a tag given that the tag was previously generated", () => {
        // TODO:
    })

    it("Should fail to verify a tag given that the tag was not previously generated", () => {
        // TODO:
    })

    it("Should require a captch for tag generation given that many tags have been generated for the organization within a short timespam", () => {
        // TODO:
    })
})